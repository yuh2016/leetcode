package com.codewars;

public class LoopInspector {
    /*public int loopSize(Node node) {
        Node next = node.getNext();
        while(node != null){
            next = node.getNext();
        }


    }*/
}
